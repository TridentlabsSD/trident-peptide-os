{
  "name": "peptrak",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx serve public -l 3000",
    "build": "echo 'No build step required'",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "devDependencies": {
    "vercel": "latest"
  },
  "engines": {
    "node": "20.x"
  }
}
